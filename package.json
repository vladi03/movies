{
  "name": "movies",
  "private": true,
  "scripts": {
    "test": "npm --prefix web test && npm --prefix mcp test",
    "ui": "npm --prefix web run dev",
    "functions": "firebase emulators:start --only functions"
  }
}